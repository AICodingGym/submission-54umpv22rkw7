# DeBERTa-v3-base：10 轮监督训练结果

完成 10 轮训练，按固定选择集的未校准 B0 QWK 选择第 **5** 轮。
最终验证集未预测、未计算指标。模型位于 `outputs_deberta_base/model/`。

## 同一选择集上的比较

全部结果使用同一批 1,557 篇选择集作文。B0 使用固定半整数阈值，
B1 在各自独立校准集上拟合阈值；不复用 small 的阈值。

| 模型 | 选择的轮次 | QWK ↑ | MAE ↓ |
| --- | ---: | ---: | ---: |
| small B0（原 4 轮实验） | 4 | 0.791044 | 0.371227 |
| small B1（原 4 轮实验） | 4 | 0.801024 | 0.400128 |
| base B0（本次 10 轮实验） | 5 | 0.814285 | 0.374438 |
| base B1（本次 10 轮实验） | 5 | 0.814573 | 0.396275 |

base B0 的 QWK 比 small B0 高 0.023241，但 MAE 略差；base B1 的
QWK 比 small B1 高 0.013550，MAE 略好。本次 base 校准只增加
0.000288 QWK，同时使 MAE 增加 0.021837。若优先平均绝对误差，
当前保存的 base 检查点宜使用 B0；以本次开发集 QWK 为准则 B1 略高。

这些是参与检查点选择和方案比较的开发集成绩，不是最终测试成绩，也不
构成显著性结论。模型规模和训练总轮数同时改变，不能把改善全部归因于
其中一个因素。10 轮调度的第 4 轮也不等价于独立训练 4 轮的模型。

## 逐轮结果

| Epoch | 训练 MSE | 选择集 B0 QWK | 选择集 B0 MAE |
| --- | ---: | ---: | ---: |
| 1 | 1.155030 | 0.793996 | 0.388568 |
| 2 | 0.373891 | 0.768732 | 0.427103 |
| 3 | 0.305160 | 0.717868 | 0.481696 |
| 4 | 0.258193 | 0.813894 | 0.353243 |
| **5** | **0.207754** | **0.814285** | **0.374438** |
| 6 | 0.159507 | 0.790507 | 0.384714 |
| 7 | 0.120103 | 0.777043 | 0.410405 |
| 8 | 0.089883 | 0.790372 | 0.385356 |
| 9 | 0.067965 | 0.775198 | 0.439949 |
| 10 | 0.053086 | 0.777704 | 0.429030 |

第 6–10 轮均未超过第 5 轮；训练损失继续下降而选择集表现变差，
符合后期过拟合的迹象。第 4 轮 MAE 最低，但预先确定的选择标准为
B0 QWK，因此保存并交付第 5 轮。逐轮指标完整保存，未将第 10 轮
当作最佳模型。

## 设置、成本与局限

- 原始模型：`microsoft/deberta-v3-base`，固定 revision
  `8ccc9b6f36199bec6961081d44eb72fb3f7353f3`。
- 10,905 篇 train 更新权重；1,557 篇 selection 选择检查点；1,557 篇
  calibration 在权重冻结后拟合阈值；保留 1,557 篇 final_validation。
- 512-token 右侧截断、attention mask 平均池化、线性回归头、FP32 MSE。
  batch 4、梯度累积 8，有效 batch 32；BF16、梯度检查点、动态 padding。
- 编码器学习率 2e-5，回归头 1e-4；10% warmup 后线性衰减，随机种子 42。
- 10 轮训练与逐轮选择集推理合计 **3,983.50 秒（66.39 分钟）**；
  不包含下载、加载、分词、模型保存、校准和最终预测导出。
- PyTorch 显存分配峰值 **3.488 GiB**，缓存保留峰值 **3.648 GiB**。
  这些数值不等于整个进程的显存占用。
- 最佳模型选择集推理 **9.717 秒**，batch 4，约 **6.24 毫秒/篇**；
  不包含加载和分词。
- 选择集依然有 **442 篇（28.39%）** 被截断。截断组 B0 MAE 为
  0.542986，未截断组为 0.307623；分数组成不同，不能把差异全部归因于
  截断。此次实验没有解决全文覆盖问题。

校准阈值：`1.824125, 2.620375, 3.734375, 4.468750, 5.680500`。
完整精度以保存的 JSON 为准。

## 使用与核验

在项目目录运行：

```bash
# B0：固定阈值；当前保存模型在选择集上的 MAE 更低
../.venv/bin/python score_deberta.py --model-dir outputs_deberta_base --device cuda --version B0 --file essay.txt
# B1：独立校准阈值；本次选择集 QWK 略高
../.venv/bin/python score_deberta.py --model-dir outputs_deberta_base --device cuda --version B1 --file essay.txt
```

[复现说明](DEBERTA.md)、[训练配置](reports/deberta_base/config.json)、
[完整训练曲线数据](reports/deberta_base/history.json)、
[指标与混淆矩阵](reports/deberta_base/report.json)、
[阈值](reports/deberta_base/thresholds.json)、
[环境与权重哈希](reports/deberta_base/environment.json)、
[独立核验](reports/deberta_base/verification.json)。

核验覆盖三个开发集合的 14,019 条预测：数据与划分哈希、ID 与标签、
整数分数、QWK、MAE、混淆矩阵及截断分组指标。独立重载全部 1,557 篇
选择集预测，连续分数和 B0/B1 整数分均与保存记录完全一致，最大连续分数差为 0。
大权重和逐篇预测保存在本地输出目录，代码与结果摘要纳入 Git。

## 平台提交

按用户指示提交第 5 轮 B1 模型的 1,731 篇测试预测，平台返回
**QWK 0.82349**。提交文件为 `outputs_deberta_base/submission.csv`，
已核对样例 ID 顺序、分数范围与校准阈值映射。
[提交记录与文件哈希](reports/deberta_base/submission.json)。
该分数来自平台测试集，与上文 1,557 篇选择集的 0.814573 是不同集合的结果。
本地保留的 final_validation 没有参与本次提交或评估。
